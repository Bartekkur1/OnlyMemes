import { SQLRepositoryBase } from "./SQLRepository";

class SQLVoteRepository extends SQLRepositoryBase implements VoteRepository {

  // @WIP
  // async voteMeme(memeId: number, up: boolean): Promise<boolean> {
  // const result = await this.client.query('Vote').insert({

  // });
  // }
}

export default class SQLVoteRepository;