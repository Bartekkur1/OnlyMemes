enum Routes {
  login = '/identity/login',
  register = '/identity/register',
  health = '/health',
  prefix = '/api',
  content = '/content',
  profile = '/profile/:displayName'
}

export default Routes;
